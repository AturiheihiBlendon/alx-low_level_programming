#include "main.h"

/**
 * _abs - computes the absolute value
 *
 * @n: to be checked
 * Return: ALwyas 0.
 */
int _abs(int)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
	return (0);
}
